import styled from "styled-components"

//TODO: build the login/Settings pages for updating the work state

const LoginForm = styled.form``;
export default function Login() {
    return (
        <LoginForm>
            
        </LoginForm>
    )
}

