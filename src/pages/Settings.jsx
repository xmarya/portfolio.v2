import { useWorkStateContext } from "../helpers/WorksState"

export default function Settings() {
    const {changeWorkState} = useWorkStateContext();
    return (
        <div>
            
        </div>
    )
}

