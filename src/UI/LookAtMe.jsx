import styled from "styled-components";

export const LookAtMe = styled.h3`
    color: var(--neon-purple);
    font-size: clamp(var(--md-text), 5vw, var(--lg-text));
    font-weight: 600;
    word-spacing: 0.2rem;
`;